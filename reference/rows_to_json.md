# JSON text for raw result rows.

JSON text for raw result rows.

## Usage

``` r
rows_to_json(rows)
```

## Arguments

- rows:

  List of row lists (`viewer_run_query()$results`).

## Value

Single JSON string (JSON array of objects).
