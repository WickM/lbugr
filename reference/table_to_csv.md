# CSV text for a table data frame.

CSV text for a table data frame.

## Usage

``` r
table_to_csv(df)
```

## Arguments

- df:

  Data frame (table view).

## Value

Character vector of CSV lines (header + data rows, no trailing newline).
