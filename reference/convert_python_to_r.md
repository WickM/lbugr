# Convert Python Objects to R Values

Internal helper function to convert Python objects (like Decimal, UUID,
nodes) to R values

## Usage

``` r
convert_python_to_r(x)
```

## Arguments

- x:

  A value that might be a Python object

## Value

An R-compatible value
