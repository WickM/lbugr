# Write a table data frame to an xlsx file.

Write a table data frame to an xlsx file.

## Usage

``` r
df_to_xlsx(df, path)
```

## Arguments

- df:

  Data frame (table view).

- path:

  Target file path.

## Value

`path` (invisible).
