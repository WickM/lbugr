% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_viewer.R
\name{graph_viewer}
\alias{graph_viewer}
\alias{check_app_deps}
\title{Launch the interactive graph viewer}
\usage{
graph_viewer(
  conn,
  port = NULL,
  host = "127.0.0.1",
  launch_browser = interactive(),
  page_size = 300L,
  load_step = 300L,
  ...
)

check_app_deps()
}
\arguments{
\item{conn}{A Ladybug connection object, as returned by \code{lb_connection()}.}

\item{port}{Port for the local Shiny server (default: pick a free one).}

\item{host}{Host for the local Shiny server.}

\item{launch_browser}{Open the app in the default browser (default:
\code{interactive()}).}

\item{page_size}{Initial number of nodes rendered in the graph.}

\item{load_step}{Increment for the "Load more" button.}

\item{...}{Further arguments passed to \code{shiny::runApp()}.}
}
\value{
The Shiny app object (invisible; the server blocks until stopped).
}
\description{
Opens a local Shiny app that lets you explore the graph of a LadybugDB
connection: g6R force-layout graph (node/relationship type filters,
N-hop focus subgraph, pagination), a reactable table view, free Cypher
queries (graph-capable results replace the graph, anything else replaces
the table), and CSV/Excel/JSON/PNG exports.
}
\details{
The app runs in-process on the current R session and uses the given
connection directly (no separate process, no HTTP backend). Write
operations (\code{CREATE}, \code{DROP}, \code{DELETE}, \code{ALTER}, \code{DETACH}, \code{MERGE}) are
blocked client-side.
}
\examples{
\dontrun{
conn <- lb_connection(":memory:")
lb_execute(conn, "CREATE NODE TABLE User(name STRING, age INT64,
PRIMARY KEY (name))")
lb_execute(conn, "CREATE (:User {name: 'Alice', age: 25})")
graph_viewer(conn)
}
}
\keyword{internal}
