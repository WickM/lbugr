% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_viewer_context.R
\name{ego_subgraph}
\alias{ego_subgraph}
\title{Extract the N-hop ego subgraph around a node.}
\usage{
ego_subgraph(node_id, graph_model, hops = 2L)
}
\arguments{
\item{node_id}{A global node id (\code{"<type>:<pk>"}) present in the model.}

\item{graph_model}{A model with \verb{$nodes} (id,label,type,color) and \verb{$edges}
(id,source,target,rel_type) data.frames, as returned by \code{rows_to_graph()}.}

\item{hops}{Number of hops (>= 1). Default 2.}
}
\value{
\code{list(nodes=, edges=, table=)} shaped like \code{rows_to_graph()}.
}
\description{
Undirected breadth-first search over \code{graph_model$edges}, starting at
\code{node_id} and expanding up to \code{hops} levels. Returns a model-shaped list
(\code{nodes}/\code{edges}/\code{table}) containing only the reachable nodes and the
edges whose endpoints are all inside the neighborhood, so the result is a
drop-in model for \code{renderG6} (which reads \verb{$nodes} and \verb{$edges}).
}
\details{
The input \code{graph_model} is the full filtered model (the module's
\code{visible_model()}), so the ego respects the active node/rel type filters.
}
\keyword{internal}
