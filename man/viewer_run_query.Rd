% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_viewer_query.R
\name{viewer_run_query}
\alias{viewer_run_query}
\title{Execute a Cypher query on a local connection and return uniform rows.}
\usage{
viewer_run_query(conn, query)
}
\arguments{
\item{conn}{A Ladybug connection from \code{lb_connection()}.}

\item{query}{A single Cypher query string (read-only; the blocklist in
\code{viewer_check_query()} is applied first).}
}
\value{
\verb{list(results = <list of row lists>, row_count = <integer>)}.
}
\description{
Shaped like the reference app's \code{run_query()}: \code{results} is a list of row
lists (column names as names), \code{row_count} the number of rows. Node and
relationship values are flattened into their property fields
(\code{n.name}, \code{r._LABEL}, ...), which is what \code{rows_to_graph()} /
\code{rows_to_df()} consume via the uniform \verb{\{type, id, label, source, target\}}
shape produced by \code{generate_graph_query()}.
}
\keyword{internal}
