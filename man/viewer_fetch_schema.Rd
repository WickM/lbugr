% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_viewer_schema.R
\name{viewer_fetch_schema}
\alias{viewer_fetch_schema}
\title{Fetch the schema of a local LadybugDB connection in \verb{/schema} shape.}
\usage{
viewer_fetch_schema(conn)
}
\arguments{
\item{conn}{A Ladybug connection object from \code{lb_connection()}.}
}
\value{
A list with \code{node_tables} (name, count, columns) and
\code{rel_tables} (name, count, connection).
}
\description{
Uses the catalog helpers of the installed ladybug Python driver
(\verb{_get_node_table_names} / \verb{_get_rel_table_names} /
\verb{_get_node_property_names} on the Connection) plus count queries via
public Cypher. Returns the same shape the reference app's
\code{GET /api/v1/schema} endpoint served, so \code{parse_schema()} consumes it
unchanged. Verified against a local seeded DB (plan D7).
}
\keyword{internal}
