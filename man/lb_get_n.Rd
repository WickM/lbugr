% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lb.R
\name{lb_get_n}
\alias{lb_get_n}
\title{Retrieve the First N Rows from a Query Result}
\usage{
lb_get_n(result, n)
}
\arguments{
\item{result}{A Ladybug query result object.}

\item{n}{The number of rows to retrieve.}
}
\value{
A list of the first \code{n} rows.
}
\description{
Fetches the first \code{n} rows from a Ladybug query result.
}
\examples{
\dontrun{
conn <- lb_connection(":memory:")
lb_execute(conn, "CREATE NODE TABLE User(name STRING, age INT64,
PRIMARY KEY (name))")
lb_execute(conn, "CREATE (:User {name: 'Alice', age: 25})")
lb_execute(conn, "CREATE (:User {name: 'Bob', age: 30})")
result <- lb_execute(conn, "MATCH (a:User) RETURN a.name, a.age")
first_row <- lb_get_n(result, 1)
}
}
