% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_viewer_model.R
\name{cap_graph}
\alias{cap_graph}
\title{Cap a graph model to its first \code{n} nodes in the model's node order.}
\usage{
cap_graph(model, n)
}
\arguments{
\item{model}{A model with \verb{$nodes} (id,...) and \verb{$edges} (id,source,target)
data.frames, as returned by \code{rows_to_graph()} / \code{ego_subgraph()}.}

\item{n}{Number of nodes to keep. If \code{n >= nrow(model$nodes)} the model is
returned unchanged.}
}
\value{
\code{list(nodes =, edges =, table =)} with at most \code{n} nodes.
}
\description{
Keeps the first \code{n} rows of \code{model$nodes} (the caller is responsible for
putting them in display order, e.g. BFS distance from a focus center) plus
only the edges whose two endpoints are both kept, so the result is a valid
drop-in model for \code{renderG6} (which reads \verb{$nodes} and \verb{$edges}). The
\code{table} is passed through untouched -- the view renders the full filtered
table, not the capped one.
}
\keyword{internal}
