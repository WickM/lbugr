% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_viewer_model.R
\name{try_rows_to_graph}
\alias{try_rows_to_graph}
\title{Try to interpret uniform query rows as a graph model (or not).}
\usage{
try_rows_to_graph(rows, schema_map)
}
\arguments{
\item{rows}{List of row lists (\code{viewer_run_query()$results}).}

\item{schema_map}{Output of \code{parse_schema()}.}
}
\value{
\code{list(ok = TRUE, model = rows_to_graph(rows, schema_map))} on
success, otherwise \verb{list(ok = FALSE, reason = <character>)}.
}
\description{
A free query is graph-capable only if its rows follow the uniform
\verb{\{type, id, label, source, target\}} shape and use rel types present in the
schema. \code{rows_to_graph()} aborts on anything else (scalar rows such as
\code{count}, an unknown rel type, or a mixed node/edge row); this wrapper turns
that abort into a value so the view can fall back to a plain result table
instead of erroring the whole query run.
}
\keyword{internal}
